import React from "react";

export const About: React.FC = () => {
  return (
    <div className="page">
      <h2>About Us!</h2>
      <p>Make sure the repo update is working in the pipeline</p>
    </div>
  );
};
